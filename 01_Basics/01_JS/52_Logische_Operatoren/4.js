let age = Number(prompt("Enter your age:"));
let money = Number(prompt("Enter the amount of money you have:"));
prompt = (age)
prompt = (money);
if (age >= 18 && money >= 12) {
    console.log("Tikcet purchased");
} else if (age >= 18 && money < 12) {
    console.log("Not enough money ");
} else {
    console.log("You are too young to watch this movie");
}